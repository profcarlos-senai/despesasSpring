meta {
  name: pega uma
  type: http
  seq: 3
}

get {
  url: {{baseurl}}/{{caminho}}/{{idNovo}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.id: eq {{idNovo}}
  res.body.nome: eq {{nomeNovo}}
}
