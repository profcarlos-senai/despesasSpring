meta {
  name: cria novo
  type: http
  seq: 3
}

post {
  url: {{baseurl}}/{{path}}
  body: json
  auth: inherit
}

body:json {
  {
    "nome": "Coisinha"
  }
}

script:post-response {
  if (res.getStatus()>=200 && res.getStatus()<=300){
    bru.setVar("idNovo",res.getBody().id);
  }
  console.log("idnovo={idNovo}");
}
