meta {
  name: cria duplicado
  type: http
  seq: 4
}

post {
  url: {{baseurl}}/{{path}}
  body: json
  auth: inherit
}

body:json {
  {
    "nome": "{{nomeNovo}}"
  }
}

assert {
  res.status: eq 409
  res.body: eq 'Já existe uma categoria com o nome "{{nomeNovo}}"'
}
